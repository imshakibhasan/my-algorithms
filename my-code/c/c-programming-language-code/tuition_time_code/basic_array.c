//#include<stdio.h>
//int main() {
    /*int a[10], i;
    for ( i=0; i<10; i++ ) {
        scanf("%d", &a[i]);
    }
    for ( i=0; i<10; i++ ) {
        printf("%d\t", a[i]);
    }*/
   /* int i, j, b[3][3];
    for ( i=0; i<3; i++ ) {
        for ( j=0; j<3; j++ ) {
            scanf("%d", &b[i][j]);
        }
    }

    for ( i=0; i<3; i++ ) {
        for ( j=0; j<3; j++ ) {
            printf("%d\t", b[i][j]);
        }
        printf("\n");
    }*/

    /*int i,j,a[3][3];
    int main_dia, upper_side, lower_side;
    printf("\nPlease Enter the number you want to put in your matrix main diagonal--");
    scanf("%d", &main_dia);
    printf("\nPlease Enter Upper side value--");
    scanf("%d",&upper_side);
    printf("\nPlease Enter Lower side value--");
    scanf("%d", &lower_side);


    for ( i=0; i<3; i++ ) {
        for ( j=0; j<3; j++ ) {
            if ( i==j ) {
                a[i][j]=main_dia;
            }
            else if ( i<j ) {
                a[i][j]=upper_side;
            }
            else {
                a[i][j]=lower_side;
            }
        }
    }
    for ( i=0; i<3; i++ ) {
        for ( j=0; j<3; j++ ) {
            printf("%d\t", a[i][j]);
        }
        printf("\n");
    }*/

/*#include<stdio.h>
int test_function(int x, int y) {

    return (x*y);
}
int main() {
    int x,y,z;
    scanf("%d", &x);
    scanf("%d", &y);
    z=test_function( x, y);
    printf("%d\t%d\t%d\n",x,y,z);
    return 0;
}*/